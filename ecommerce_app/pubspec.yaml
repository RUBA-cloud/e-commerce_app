name: ecommerce_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"   # use a range compatible with your installed Flutter

dependencies:
  flutter:
    sdk: flutter

  # UI & state
  cupertino_icons: ^1.0.8
  get: ^4.7.2
  flutter_bloc: ^8.1.3
  flutter_screenutil: ^5.9.3
  image_picker: ^1.1.2

  # Firebase & auth
  firebase_core: any
  firebase_auth: any
  firebase_messaging: ^15.0.0
  google_sign_in: ^6.3.0
  flutter_facebook_auth: any
  sign_in_with_apple: any

  # Utils
  font_awesome_flutter: ^10.7.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.5.3

  # 🔑 Flutter localization support
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
